If you have access to the controller (so the safe is unlocked) but
the software still thinks there's a lock running then this sketch
will delete the lockID.
    
You can then re-upload the main sketch
